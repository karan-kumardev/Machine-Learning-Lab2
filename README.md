**Machine Learning Data Cleaning before Training model**

use the **Telco_customer_churn** to know the dataset and then apply data cleaning methods using "Pandas" library

then generate the cleaned dataset in the end named as **clean_churn** to be used for training the model.

provided the **.ipynb** files contains the required questions and methods to clean the data and make it **ML** ready
